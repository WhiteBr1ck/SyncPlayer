# SyncPlayer

SyncPlayer 是一个 Windows 便携式 mpv 启动器，用于本机多屏视频同步播放和局域网多电脑同步播放。

## 功能

- 打开一个视频文件，并启动一个或多个 mpv 播放窗口。
- 同步播放、暂停、跳转进度、全屏、窗口化和关闭操作。
- 支持同一台电脑上的本机多屏同步。
- 支持局域网内多台电脑运行 SyncPlayer 后同步播放。

## 局域网同步

主机电脑：

- 启用局域网多屏同步。
- 选择作为主机。
- 设置监听地址和端口。
- 保存局域网设置。

从机电脑：

- 启用局域网多屏同步。
- 选择作为从机。
- 输入主机 IP 地址和端口。
- 测试连接。
- 保存局域网设置。

注意：所有电脑需要能访问相同的视频文件路径（比如局域网SMB网络路径）。

## 本地配置

用户设置保存在：

```text
syncplayer.json
```

## 更新日志

### 0.0.7

- 新增播放结束后行为选项：暂停在最后一帧、播放下一集、循环播放。
- 新增播放设置里的同步纠正阈值滑块和恢复默认按钮。
- 新增全部静音选项；启用后会禁用第 2 个及之后默认静音选项。
- 局域网同步打开视频时同步字幕、静音、硬件解码和播放结束后行为等播放选项。
- 修复关闭字幕选项不会同步到局域网从机的问题。
- 修复播放下一集时主副播放器可能进入不同文件或同时关闭的问题。
- 修复暂停最后一帧在播放列表中间项会自动跳下一集的问题。
